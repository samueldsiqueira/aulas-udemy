const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2);
/*Math é um objeto ele tem uma constante
 chamada PI e tem uma função que calcula 
 a potencia valor "pow"*/

console.log(area);
console.log(typeof Math)